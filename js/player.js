(function(global){
class EnhancedPlayer{constructor(video,ui,statsEl){this.video=video;this.ui=ui;this.statsEl=statsEl;this.stream=null;this.title='';this.uiTimer=null;this.statsTimer=null;this.warmResult=null;this.boundUpdate=()=>this.renderStats()}
async open(stream,title){const url=NuvioStreams.sourceUrl(stream);if(!url)throw new Error('Esta fonte não fornece URL reproduzível pelo player nativo.');this.stream=stream;this.title=title||'';document.getElementById('player-title').textContent=this.title;document.getElementById('player-source').textContent=(stream._addonName||'')+' · '+NuvioStreams.displayName(stream);document.getElementById('player-overlay').classList.remove('hidden');this.showUI();if(localStorage.getItem('nuvioEnhanced.statsOpen')==='true')this.statsEl.classList.remove('hidden');this.warmResult=await NuvioStreams.warm(url,1800);this.video.src=url;this.video.load();try{await this.video.play()}catch(_){}clearInterval(this.statsTimer);this.statsTimer=setInterval(this.boundUpdate,1000);this.renderStats()}
close(){clearInterval(this.statsTimer);this.video.pause();this.video.removeAttribute('src');this.video.load();this.stream=null;this.statsEl.classList.add('hidden');document.getElementById('player-overlay').classList.add('hidden')} toggle(){if(this.video.paused)this.video.play();else this.video.pause();this.showUI()} seek(seconds){if(Number.isFinite(this.video.duration))this.video.currentTime=Math.max(0,Math.min(this.video.duration,this.video.currentTime+seconds));this.showUI()} toggleStats(){this.statsEl.classList.toggle('hidden');this.renderStats()} showUI(){this.ui.classList.add('visible');clearTimeout(this.uiTimer);this.uiTimer=setTimeout(()=>this.ui.classList.remove('visible'),4500)}
bufferedAhead(){const v=this.video;try{for(let i=0;i<v.buffered.length;i++)if(v.currentTime>=v.buffered.start(i)&&v.currentTime<=v.buffered.end(i))return Math.max(0,v.buffered.end(i)-v.currentTime)}catch(_){}return 0} estimatedBitrateMbps(){const bytes=Number(this.stream&&this.stream.behaviorHints&&this.stream.behaviorHints.videoSize||0);const duration=Number(this.video.duration||0);return bytes&&duration?bytes*8/duration/1e6:0} playbackQuality(){try{return this.video.getVideoPlaybackQuality?this.video.getVideoPlaybackQuality():{}}catch(_){return{}}}
renderStats(){if(!this.stream)return;const v=this.video,q=this.playbackQuality(),ahead=this.bufferedAhead(),est=this.estimatedBitrateMbps(),text=[this.stream.title,this.stream.name,this.stream.description].filter(Boolean).join(' ');const host=(()=>{try{return new URL(NuvioStreams.sourceUrl(this.stream)).host}catch(_){return'—'}})();const hdr=/dolby[ .-]?vision|\bdv\b/i.test(text)?'Dolby Vision (fonte)':/hdr10\+|hdr10plus/i.test(text)?'HDR10+ (fonte)':/\bhdr\b/i.test(text)?'HDR (fonte)':'SDR/indefinido';const codec=/av1/i.test(text)?'AV1':/hevc|x265|h\.265/i.test(text)?'HEVC/H.265':/x264|h\.264|avc/i.test(text)?'AVC/H.264':'indefinido';const audio=/truehd.*atmos|atmos.*truehd/i.test(text)?'TrueHD Atmos':/truehd/i.test(text)?'TrueHD':/dts[ .-]?hd/i.test(text)?'DTS-HD':/dts:x/i.test(text)?'DTS:X':/eac3|dd\+/i.test(text)?'E-AC-3':/ac3|dd\b/i.test(text)?'AC-3':'indefinido';const rows=[['Resolução',v.videoWidth&&v.videoHeight?v.videoWidth+'×'+v.videoHeight:'aguardando'],['Qualidade fonte',NuvioStreams.qualityLabel(this.stream)],['Vídeo',codec],['HDR/DV',hdr],['Áudio',audio],['Buffer',ahead.toFixed(1)+' s'],['Bitrate estimado',est?est.toFixed(1)+' Mbps':'indefinido'],['Warm-up',this.warmResult&&this.warmResult.ok?this.warmResult.ms.toFixed(0)+' ms'+(this.warmResult.mbps?' · '+this.warmResult.mbps.toFixed(1)+' Mbps':''):'não mensurável'],['Frames perdidos',String(q.droppedVideoFrames==null?'—':q.droppedVideoFrames)],['Ready state',String(v.readyState)],['Host',host],['Addon',this.stream._addonName||'—']];this.statsEl.innerHTML='<strong>Stats for Nerds · webOS</strong>'+rows.map(r=>'<div class="stats-row"><span>'+escapeHtml(r[0])+'</span><span>'+escapeHtml(r[1])+'</span></div>').join('');document.getElementById('playpause').textContent=v.paused?'Play':'Pause';document.getElementById('time-label').textContent=fmt(v.currentTime)+' / '+fmt(v.duration)}}
function fmt(s){if(!Number.isFinite(s))return'00:00';const h=Math.floor(s/3600),m=Math.floor((s%3600)/60),sec=Math.floor(s%60);return(h?h+':'+String(m).padStart(2,'0'):String(m).padStart(2,'0'))+':'+String(sec).padStart(2,'0')} function escapeHtml(v){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))} global.EnhancedPlayer=EnhancedPlayer;})(window);
