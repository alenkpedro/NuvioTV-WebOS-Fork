name: Build LG webOS IPK

on:
  push:
    branches: [main]
    paths-ignore:
      - 'webosbrew/apps.json'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: webos-release-main
  cancel-in-progress: true

jobs:
  build-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Validate JavaScript
        run: npm run check

      - name: Build IPK
        run: npm run package:webos

      - name: Resolve immutable package name
        id: pkg
        shell: bash
        run: |
          VERSION="$(node -p "require('./appinfo.json').version")"
          SOURCE="dist/Nuvio-Enhanced-webOS-${VERSION}.ipk"
          test -f "$SOURCE"
          SHORT_SHA="${GITHUB_SHA::12}"
          IMMUTABLE="dist/Nuvio-Enhanced-webOS-${VERSION}-${SHORT_SHA}.ipk"
          mv "$SOURCE" "$IMMUTABLE"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "ipk=$IMMUTABLE" >> "$GITHUB_OUTPUT"
          echo "filename=$(basename "$IMMUTABLE")" >> "$GITHUB_OUTPUT"

      - name: Publish immutable prerelease asset
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if gh release view webos-latest >/dev/null 2>&1; then
            gh release upload webos-latest "${{ steps.pkg.outputs.ipk }}"
            gh release edit webos-latest \
              --title "Nuvio webOS ${{ steps.pkg.outputs.version }}" \
              --notes "Automatic LG webOS development build from main. UI and behavior aligned with ysosrs123/NuvioTV-Fork; webOS platform layer adapted for LG TV." \
              --prerelease
          else
            gh release create webos-latest \
              "${{ steps.pkg.outputs.ipk }}" \
              --target "${GITHUB_SHA}" \
              --title "Nuvio webOS ${{ steps.pkg.outputs.version }}" \
              --notes "Automatic LG webOS development build from main. UI and behavior aligned with ysosrs123/NuvioTV-Fork; webOS platform layer adapted for LG TV." \
              --prerelease
          fi

      - name: Generate Homebrew repository manifest from published asset
        env:
          RELEASE_TAG: webos-latest
        run: node scripts/generate-webosbrew.mjs "${{ steps.pkg.outputs.ipk }}"

      - name: Commit Homebrew manifest
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add webosbrew/apps.json
          if git diff --cached --quiet; then
            echo "Homebrew manifest unchanged"
            exit 0
          fi
          git commit -m "chore: update webOS Homebrew manifest [skip ci]"
          git pull --rebase origin main
          git push

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: Nuvio-webOS-${{ steps.pkg.outputs.version }}-${{ github.sha }}
          path: ${{ steps.pkg.outputs.ipk }}
          if-no-files-found: error
